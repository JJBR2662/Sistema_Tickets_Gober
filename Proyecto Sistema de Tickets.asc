Proyecto Sistema de Tickets v1.3
================================
:doctype: book
:docinfo:
:lang: es
:toc: left
:toc-title: Índice de Contenido
:toclevels: 4
:pagenums:
:chapter-label: Capítulo
:icons: font
:imagesdir: imagenes
:sectnums:
:sectnumlevels: 4
:leveloffset: 1
:source-highlighter: highlightjs
:source-linenums-option: enable

icon:warning[] [black yellow-background]*NOTA 1:* Las *NOTAS* [red]**NO** deben ser incluidas en este trabajo, así como las *imagenes* que no se utilicen. 

= icon:wrench[] Recursos

image::asciidoctor-logo-bl.svg[align="center",width=300]

[%header,cols=">,<"]

|===
|Tipo
|Valor

|Formato
|icon:pencil[] https://asciidoctor.org/[Asciidoctor]

|Herramienta
|icon:edit[] https://code.visualstudio.com/[Visual Studio Code]

|Extensiones de VSC
|icon:plug[] https://marketplace.visualstudio.com/items?itemName=asciidoctor.asciidoctor-vscode[AsciiDoc]

.^|Referencias
|icon:book[] https://docs.asciidoctor.org/[Asciidoctor Documentation Home]

icon:book[] https://nelbren.com/es/documentation/Documentation_with_Markdown_Marp_and_AsciiDoctor/[Documentación con Markdown, Marp y AsciiDoctor]

|===

= icon:check-square[] Rúbrica

image::rúbrica.png[align="center",width=300]

[%header%footer,cols=">.^1,.^5,>.^1"]
|===
|icon:list[] Inciso
|icon:comment[] Descripción
|icon:check[] Puntos

3+^|icon:database[] *BACKEND*

|1
|Imagenes
|2

|2
|Instrucciones
|2


3+^|icon:code[] *API*

|3
|Código
|2

|4
|Imagenes
|2

3+^|icon:laptop[] *FRONTEND*

|5
|Código
|2

|6
|Imagenes
|2

|6
|Envio de texto (asunto)
|1

|7
|Envio de imagen (captura de pantalla)
|1


3+^|icon:wrench[] *GESTIÓN*

|8
|Imagenes
|2

3+^|icon:book[] *DOCUMENTACIÓN*

|9
|Formato AsciiDoctor
|2

|10
|Redacción y ortografía
|2


2+^|Total
>|*20*
|

|===

icon:warning[] [black yellow-background]*NOTA 2:* Las secciones de los *recursos* y la *rúbrica* [red]**NO** deben ser incluidos en este trabajo.

= icon:code[] Envió de Tickets por medio de API hacia Redmine

== icon:ticket[] Redmine

image::https://www.redmine.org/attachments/download/3458/redmine_logo_v1.png[align="center"]

=== icon:info[] Descripción

*Redmine* es una plataforma web de código abierto para la gestión de proyectos e incidencias. En este proyecto se utiliza como el *backend* del sistema de tickets: recibe las solicitudes HTTP de la interfaz, valida los permisos del usuario, registra las incidencias y las conserva en una base de datos.

La instalación se realizó de forma local en *Debian GNU/Linux 13.5* ejecutado mediante WSL. La arquitectura implementada es la siguiente:

[source,text]
----
Navegador en Windows
        |
        | http://localhost
        v
Apache HTTP Server + Phusion Passenger
        |
        v
Redmine 6.0.5
        |
        v
MariaDB
----

=== icon:copyright[] Licencia

Redmine se distribuye como software libre bajo la licencia *GNU General Public License versión 2 (GPL-2.0)*. Esta licencia permite usar, estudiar, modificar y redistribuir el software, siempre que se mantengan las condiciones de la licencia.

=== icon:gear[] Desarrollo

==== icon:server[] BACKEND

El backend se configuró con Redmine, MariaDB, Apache y Phusion Passenger. MariaDB almacena los datos persistentes de los proyectos y tickets; Apache publica la aplicación web y Passenger ejecuta la aplicación Ruby on Rails de Redmine.

===== icon:download[] Instalación

La instalación se realizó dentro de Debian GNU/Linux 13.5 en WSL. Primero se actualizaron los índices de paquetes y luego se instalaron Redmine, su soporte para MariaDB/MySQL, Apache y Passenger.

====== icon:picture-o[] Capturas de pantalla

. Paquetes principales instalados
+
La evidencia muestra los paquetes instalados con estado `ii`: Apache 2.4.67, Passenger 6.0.26, MariaDB 11.8.6, Redmine 6.0.5 y el paquete de soporte `redmine-mysql`.
+
image::backend_paquetes_instalados.png[align="center",width=1000]

. Acceso local inicial a Redmine
+
Después de completar la instalación y la publicación con Apache, Redmine responde en la dirección local `http://localhost`.
+
image::backend_redmine_inicio.png[align="center",width=1000]

====== icon:terminal[] Instrucciones

[source,bash]
----
# Actualizar la información de paquetes y el sistema
sudo apt update
sudo apt upgrade -y

# Instalar los componentes del backend
sudo apt install -y \
  mariadb-server \
  apache2 \
  libapache2-mod-passenger \
  redmine-mysql \
  curl

# Activar e iniciar los servicios necesarios
sudo systemctl enable --now mariadb
sudo systemctl enable --now apache2
----

Los paquetes instalados fueron los siguientes:

* `mariadb-server`: servidor de base de datos.
* `apache2`: servidor HTTP que expone Redmine en el puerto 80.
* `libapache2-mod-passenger`: módulo que permite a Apache ejecutar Redmine.
* `redmine` y `redmine-mysql`: aplicación de tickets y su soporte para MariaDB/MySQL.

===== icon:wrench[] Configuración

La configuración se realizó en tres niveles: publicación de Redmine con Apache y Passenger, acceso administrativo inicial y habilitación de la API REST para la integración posterior con el frontend.

====== icon:picture-o[] Capturas de pantalla

. Base de datos de la instancia de Redmine
+
La instalación por paquetes de Debian creó la base de datos `redmine_default` en MariaDB. Esta base de datos conserva la información de usuarios, proyectos, tickets y configuraciones de la instancia.
+
image::backend_base_datos_redmine_default.png[align="center",width=1000]

. Habilitación de Passenger y publicación del sitio de Redmine en Apache
+
La siguiente evidencia muestra la activación del módulo Passenger, la habilitación del sitio `redmine.conf`, la desactivación del sitio predeterminado y la validación de la configuración de Apache con `Syntax OK`. Durante la comprobación inicial se mostró una advertencia de Passenger relacionada con el alcance de una directiva; no impidió que Apache recargara la configuración. La evidencia posterior de acceso a Redmine confirma que la publicación quedó operativa.
+
image::backend_apache_passenger_configuracion.png[align="center",width=1000]

. Cuenta administrativa configurada
+
image::backend_cuenta_admin.png[align="center",width=1000]

. API REST habilitada desde `Administration > Settings > API`
+
image::backend_api_rest_habilitada.png[align="center",width=1000]

. Permisos seleccionados para el rol `Emisor API`
+
image::backend_permisos_emisor_api_1.png[align="center",width=750]
+
image::backend_permisos_emisor_api_2.png[align="center",width=750]

. Rol `Emisor API` creado en Redmine
+
image::backend_rol_emisor_api.png[align="center",width=1000]

====== icon:terminal[] Instrucciones

Primero se creó un sitio de Apache que apunta a la carpeta pública de Redmine. La variable `REDMINE_INSTANCE` indica que se utiliza la instancia predeterminada configurada por el paquete de Debian. La directiva `PassengerDefaultUser` no se coloca dentro del bloque `<VirtualHost>` porque Apache emite una advertencia cuando se configura en ese alcance; Passenger utiliza la configuración global instalada por el paquete de Debian.

[source,bash]
----
sudo tee /etc/apache2/sites-available/redmine.conf > /dev/null <<'EOF'
<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /usr/share/redmine/public

    RailsEnv production
    SetEnv REDMINE_INSTANCE "default"

    PassengerEnabled on
    PassengerAppRoot /usr/share/redmine
    PassengerAppEnv production
    PassengerAppGroupName redmine_default

    <Directory /usr/share/redmine/public>
        Options -MultiViews
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/redmine_error.log
    CustomLog ${APACHE_LOG_DIR}/redmine_access.log combined
</VirtualHost>
EOF

sudo a2enmod passenger
sudo a2dissite 000-default.conf
sudo a2ensite redmine.conf
sudo apache2ctl configtest
sudo systemctl restart apache2
----

Después se ingresó a `http://localhost` con la cuenta inicial `admin`, se actualizó la contraseña administrativa y se habilitó la opción *Enable REST web service* en `Administration > Settings > API`.

También se creó el rol `Emisor API`. Para la operación básica del formulario se requieren, como mínimo, los permisos *View issues* y *Add issues*. El permiso *Add notes* solo es necesario si la aplicación permitirá agregar comentarios a tickets existentes.

===== icon:terminal[] Ejecución

Para ejecutar el backend se inician MariaDB y Apache. Posteriormente se abre Redmine en el navegador usando la dirección local `http://localhost`.

====== icon:picture-o[] Captura de pantalla

La pantalla siguiente confirma el acceso administrativo a la instancia de Redmine después de iniciar sesión.

image::backend_cuenta_admin.png[align="center",width=1000]

====== icon:terminal[] Instrucciones

[source,bash]
----
# Iniciar los servicios si no están activos
sudo systemctl start mariadb
sudo systemctl start apache2

# Verificar que ambos servicios estén activos
systemctl is-active mariadb
systemctl is-active apache2

# Comprobar la sintaxis de Apache
sudo apache2ctl configtest

# Abrir Redmine desde Windows
# http://localhost
----

La respuesta esperada para los dos servicios es `active` y Apache debe mostrar `Syntax OK`. Una vez disponible la interfaz, Redmine puede administrarse desde el navegador mediante la cuenta administrativa.

===== icon:check[] Verificación

La verificación comprueba la versión del sistema operativo, las versiones instaladas del backend y el estado de los servicios. Durante la instalación se confirmó Debian GNU/Linux 13.5, Redmine 6.0.5, Apache 2.4.67 y MariaDB 11.8.6.

====== icon:picture-o[] Captura de pantalla

La siguiente evidencia reúne la salida de los comandos de comprobación. Se observa que `apache2` y `mariadb` están `active`, Apache devuelve `Syntax OK` y la solicitud local responde `HTTP/1.1 200 OK`. Por tanto, Redmine quedó disponible y publicado mediante Apache y Passenger.

image::backend_verificacion_completa.png[align="center",width=1000]


====== icon:terminal[] Instrucciones

[source,bash]
----
# Versión del sistema operativo
cat /etc/os-release
cat /etc/debian_version

# Versiones de los paquetes principales
printf 'Redmine: '
dpkg-query -W -f='${Version}\n' redmine

printf 'Apache: '
dpkg-query -W -f='${Version}\n' apache2

printf 'MariaDB: '
dpkg-query -W -f='${Version}\n' mariadb-server

# Estado de los servicios
systemctl is-active apache2
systemctl is-active mariadb

# Validación de Apache y prueba HTTP local
sudo apache2ctl configtest
curl -I http://localhost
----

La instalación queda validada cuando se cumplen las siguientes condiciones:

* Debian reporta la versión `13.5`.
* Los paquetes de Redmine, Apache y MariaDB aparecen instalados.
* `apache2` y `mariadb` responden `active`.
* Apache responde `Syntax OK`.
* Redmine se muestra correctamente en `http://localhost`.

==== icon:plug[] API

image::https://assets.goodfirms.co/glossary/1655457664-api.png[align="center"]

===== icon:info[] Descripción

*API* es ..

===== icon:cube[] Herramientas

- icon:plug[] https://marketplace.visualstudio.com/items?itemName=humao.rest-client[REST Client - Extensión de Visual Studio Code] 
- u otro que se desee utilizar que sea similar a https://www.postman.com/[Postman]
+
icon:warning[] [black yellow-background]*NOTA 5:* En el caso de usar otro cliente para las pruebas, favor documentar los pasos necesarios para realizar la prueba en su totalidad.

===== icon:list[] Listado antes de la prueba de envió

====== icon:code[] Código

- icon:gear[] list.http
+
[source,html]
----
include::fuente/list.http[]
----

====== icon:picture-o[] Captura de pantalla

- icon:gear[] list.http
+
image::api_test_01.png[align="center",width=1000]

===== icon:send[] Prueba de envió

====== icon:code[] Código

- icon:gear[] send.http
+
[source,html]
----
include::fuente/send.http[]
----

====== icon:picture-o[] Captura de pantalla

- icon:gear[] send.http
+
image::api_test_02.png[align="center",width=1000]

===== icon:list[] Listado después de la prueba de envió

====== icon:code[] Código

- icon:gear[] list.http
+
[source,html]
----
include::fuente/list.http[]
----

====== icon:picture-o[] Captura de pantalla

- icon:gear[] list.http
+
image::api_test_03.png[align="center",width=1000]

- icon:ticket[] https://npr3s.com/control/projects/gti-0868-0498/issues[Peticiones en Proyecto gti-0868-0498 de Redmine]
+
image::api_test_04.png[align="center",width=1000]

==== icon:code[] FRONTEND

===== icon:github[] Clonación

[source,bash]
----
git clone https://github.com/JJBR2662/Sistema_Tickets_Gober.git
----

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:download[] Instalación

[source,bash]
----
No requiere instalación de dependencias. Solo se necesita un navegador web como Chrome, Firefox, Edge, etc.
----

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:terminal[] Ejecución

[source,bash]
----
start frontend/index.html
----

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:send[] Prueba de envió

====== icon:picture-o[] Captura de pantalla

- icon:gear[] App
+
image::imagen_frontend_UI.png[align="center",width=1000]

===== icon:list[] Verificación de envió

====== icon:picture-o[] Captura de pantalla

- icon:ticket[] https://npr3s.com/control/projects/gti-0868-0498/issues[Peticiones en Proyecto gti-0868-0498 de Redmine]
+
image::app_test_02.png[align="center",width=1000]

===== icon:code[] Código del App

- fuente/frontend/index.html
+
[source,html]
----
include::frontend/index.html[]
----

- fuente/frontend/styles.css
+
[source,css]
----
include::frontend/styles.css[]
----

- fuente/frontend/app.js
+
[source,javascript]
----
include::frontend/app.js[]
----

- fuente/frontend/app.ts
+
[source,typescript]
----
include::frontend/app.ts[]
----

=== icon:gear[] Gestión

==== icon:book[] Proyecto 1/2

===== icon:plus[] Creación

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::creacion_de_proyecto.png[align="center",width=1000]

==== icon:ticket[] Peticiones

===== icon:eye[] Ver

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::ver_peticiones.png[align="center",width=1000]

===== icon:edit[] Modificar

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::modificar_peticiones.png[align="center",width=1000]

===== icon:star[] Priorizar

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::priorizar_peticiones.png[align="center",width=1000]

===== icon:wrench[] Trabajo

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::trabajo_peticiones.png[align="center",width=1000]

===== icon:trash[] Eliminar

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::eliminar_peticiones.png[align="center",width=1000]

==== icon:book[] Proyecto 2/2

===== icon:edit[] Modificar

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::modificar_proyecto.png[align="center",width=1000]

===== icon:trash[] Eliminar

icon:warning[] [black yellow-background]*NOTA 4:* Aquí va(n) una(s) captura(s) de pantalla(s)

===== icon:picture-o[] Captura de pantalla

- Evidencia
+
image::eliminar_proyecto.png[align="center",width=1000]

== icon:users[] Autor

[%header,cols=">,<,>"]
|===

|icon:hashtag[] Cuenta
|icon:user[] Nombre
|icon:percent[] Participación

|icon:hashtag[] 00000001
|icon:user[] Nombre de Alumno
|icon:percent[] 100%

|icon:hashtag[] 12251021
|icon:user[] Carlos Flores
|icon:percent[] 100%

|icon:hashtag[] 0000000n
|icon:user[] Nombre de Alumno
|icon:percent[] 100%

|===

== icon:book[] Referencias

- icon:link[] https://docs.asciidoctor.org/[Asciidoctor Documentation Home]
- ...

[.text-center]
**Desarrollado** para *Gobernabilidad de Tecnologías de Información | UNITEC*